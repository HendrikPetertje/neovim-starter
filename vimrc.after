" Plugins: Yankring ----------------------------------------------------------
" make Y consistent with C and D
" must be in 'after' to prevent yankring overriding it
nnoremap Y y$

" Plugins: CTRLP Delete buffer -----------------------------------------------
if filereadable(expand("~/.config/nvim/plugged/ctrlp_bdelete.vim/autoload/ctrlp_bdelete.vim"))
  call ctrlp_bdelete#init()
endif
